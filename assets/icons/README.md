# Icons

Optional folder for any custom icon files (e.g. a PWA icon set) you want to
add later. The site currently uses a plain emoji/text favicon defined inline
in each page's `<head>`, so nothing here is required for the site to work.
